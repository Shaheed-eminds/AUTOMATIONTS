# Copy this file to .env and adjust as needed.
# .env is git-ignored so local overrides never get committed.

# ONBOARDLY_APP_URL: optional override. Defaults to apps/onboardly-app.html in this repo.
